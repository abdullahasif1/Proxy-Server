import requests
import socket

atuple = (50006, 1, 0, 0, 1000)
#host = 'edition.cnn.com'
#ipAddress = socket.gethostbyname(host)
#c.connect((ipAddress,443))
#the required first parameter of the 'get' method is the 'url':
#x = requests.get('https://edition.cnn.com')
#c.sendall(bytes('https://edition.cnn.com', 'utf

#resp = c.recv(1000).decode()
#print(resp.body)

print(type(atuple[0]))

